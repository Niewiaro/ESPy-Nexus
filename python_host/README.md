# ESPy-Nexus

## Logging directory (.env)

You can override the logs directory with an environment variable:

```
ESPY_NEXUS_LOG_DIR=logs
```

Create a local `.env` file based on `.env.example`.
If the variable is missing or empty, the app falls back to `logs`.
