\chapter{Architektura i implementacja webowego środowiska analitycznego}

Rozbudowana matryca badawcza, składająca się z wielu wariantów obciążeniowych, badanych protokołów komunikacyjnych oraz topologii sieci radiowej, wymagała opracowania dedykowanego środowiska analitycznego.
Praca układu doprowadzająca do saturacji pasma radiowego generowała miliony punktów pomiarowych, które należało w sposób uporządkowany rejestrować, agregować i wizualizować.
W tym celu powstała autorska platforma analityczna, której architektura i implementacja zostały omówione w niniejszym rozdziale.

O ile zaimplementowany w języku Python moduł głównego silnika testowego (TestEngine) doskonale radzi sobie z rygorystyczną orkiestracją cyklu życia testów i akwizycją danych, a analizator wsadowy (BatchAnalyzer) umożliwia wydajną obróbkę stempli czasowych zgodnie z paradygmatem ETL (\textit{ang. Extract, Transform, Load}), to ostatecznym rezultatem tych operacji jest obszerna baza danych lub zbiór płaskich plików CSV.
W tej surowej postaci dane nie sprzyjają szybkiej interpretacji i pozostają mało czytelne dla użytkownika końcowego.
Analiza wyników w formie wyłącznie tabelarycznej jest procesem nieefektywnym i podatnym na błędy.
Aby umożliwić sprawną i obiektywną ocenę wydajności badanych protokołów, konieczne stało się zaprojektowanie dedykowanego, graficznego interfejsu użytkownika (\textit{ang. GUI -- Graphical User Interface}), który w sposób interaktywny prezentowałby zagregowane wyniki pomiarów.

Język Python oferuje wiele dojrzałych bibliotek umożliwiających tworzenie wizualizacji danych, takich jak \textit{matplotlib} \cite{matplotlib}, \textit{seaborn} \cite{seaborn} czy \textit{plotly} \cite{plotly}.
Interaktywne panele i aplikacje oparte na danych można z kolei budować m.in. z wykorzystaniem notatników \textit{Jupyter Notebook} \cite{jupyter} oraz frameworka \textit{Streamlit} \cite{streamlit}.
Choć narzędzia te doskonale sprawdzają się w fazie prototypowania i eksploracji danych, to w kontekście niniejszej pracy, której celem jest dostarczenie wydajnego narzędzia analitycznego dla inżynierów systemów wbudowanych, ich możliwości architektoniczne okazały się niewystarczające.
Kryteria doboru docelowego narzędzia wizualizacyjnego obejmowały przede wszystkim:
\begin{itemize}
      \item pełną dostępność w przeglądarce internetowej, bez konieczności konfiguracji środowiska uruchomieniowego czy instalacji dodatkowego oprogramowania na stacji roboczej,
      \item możliwość zdalnego dostępu do wyników pomiarów,
      \item wysoką interaktywność wizualizacji, umożliwiającą dynamiczne filtrowanie serii danych, elastyczne dostosowywanie zakresu badanych częstotliwości i natychmiastowe porównywanie topologii,
      \item elastyczną integrację z istniejącym silnikiem testowym i analizatorem wsadowym,
      \item rygorystyczną separację warstwy prezentacji od logiki biznesowej (\textit{ang. Separation of Concerns}), gwarantującą niezależny rozwój i utrzymanie obu komponentów (skrypty w środowisku Python oraz mikrokontroler ESP32 odpowiadają w tym modelu wyłącznie za wysokoczęstotliwościową komunikację sieciową, kontrolę pętli HIL oraz deterministyczne wyliczanie metryk).
\end{itemize}

Powyższe wymagania sprawiły, że natywne narzędzia dostępne w ekosystemie języka Python ustąpiły miejsca nowoczesnemu frameworkowi webowemu.
Dostępność wyników analizy bezpośrednio w silniku przeglądarki jest współcześnie standardem nie tylko w systemach biznesowych, ale i w specjalistycznych narzędziach inżynierskich.
Rozwiązanie to pozwala na błyskawiczne porównywanie wyników i bezproblemowe współdzielenie wniosków z pozostałymi członkami zespołu oraz interesariuszami projektu, wykorzystując powszechnie dostępne przeglądarki na urządzeniach stacjonarnych i mobilnych.

\section{Stos technologiczny aplikacji webowej}

\subsection{Środowisko wykonawcze Python}

Projektując interaktywne narzędzie do analizy wyników z systemu Hardware-in-the-Loop, rozważono kilka możliwych podejść architektonicznych.
Pierwszym i najbardziej naturalnym podejściem, ze względu na wykorzystanie języka Python w głównym silniku testowym, było zastosowanie jego ekosystemu.
Narzędzia takie jak \textit{Streamlit}, \textit{Dash} (oparty na bibliotece Plotly) czy \textit{Panel} pozwalają na szybkie prototypowanie interfejsów analitycznych bez konieczności bezpośredniego tworzenia warstwy interfejsu z wykorzystaniem technologii webowych, takich jak HTML, CSS czy JavaScript.

Mimo licznych zalet, rozwiązania te posiadają ograniczenia architektoniczne istotne z punktu widzenia docelowej aplikacji.
W typowej konfiguracji rozwiązania te wykorzystują architekturę klient--serwer, w której logika aplikacji napisana w Pythonie jest wykonywana po stronie serwera, natomiast przeglądarka odpowiada za prezentację interfejsu i obsługę interakcji użytkownika.
W przypadku \textit{Streamlit} interakcja użytkownika z elementami interfejsu może powodować ponowne wykonanie skryptu Python, co w przypadku kosztownych operacji na dużych zbiorach danych może wpływać na responsywność interfejsu.
Mechanizmy takie jak buforowanie wyników oraz zarządzanie stanem sesji pozwalają ograniczyć ten problem, jednak nie zmieniają podstawowego modelu wykonywania aplikacji.

Istotnym ograniczeniem z perspektywy projektowanego narzędzia jest również konieczność utrzymywania środowiska wykonawczego po stronie serwera.
Aplikacja wykorzystująca Python nie może zostać wdrożona jako wyłącznie statyczna aplikacja webowa, ponieważ jej logika wymaga uruchomionego interpretera Python oraz serwera obsługującego komunikację z klientem.
W konsekwencji rozwiązanie tego typu wymaga infrastruktury serwerowej, podczas gdy aplikacja zaprojektowana z myślą o wykonywaniu logiki bezpośrednio w przeglądarce może zostać wdrożona jako zbiór statycznych zasobów i dystrybuowana za pośrednictwem sieci CDN.

\subsection{HTML z JavaScript}

Drugim rozważanym podejściem było zrezygnowanie z aplikacji serwerowej i implementacja interfejsu z wykorzystaniem języka JavaScript bez dodatkowej warstwy abstrakcji w postaci frameworka (ang.
Vanilla JS).
Ponieważ warstwa wizualizacyjna wykorzystuje bibliotekę Chart.js, a dane wejściowe są przechowywane w plikach JSON, możliwe byłoby zrealizowanie całej aplikacji z wykorzystaniem standardowych interfejsów programistycznych udostępnianych przez przeglądarkę.

Podejście to jest jednak mniej korzystne wraz ze wzrostem złożoności interfejsu.
Projektowane narzędzie wymaga obsługi wielu wzajemnie zależnych elementów interaktywnych, obejmujących między innymi grupowanie danych, przyciski szybkiego wyboru, dynamiczne ukrywanie serii oraz odrębną zakładkę z tabelarycznym zestawieniem wyników.
W przypadku implementacji bez wykorzystania frameworka programista musi samodzielnie zaprojektować mechanizmy zarządzania stanem aplikacji oraz synchronizacji tego stanu z warstwą prezentacji.
Oznacza to konieczność ręcznej obsługi zmian interfejsu, aktualizacji wykresów oraz zależności pomiędzy poszczególnymi elementami widoku.
Wraz ze wzrostem liczby takich zależności zwiększa się złożoność kodu, a także ryzyko powstawania trudnych w utrzymaniu zależności i niespójności stanu aplikacji.

Dodatkowym aspektem jest konieczność samodzielnego zarządzania cyklem życia dynamicznie tworzonych elementów interfejsu oraz powiązanych z nimi zasobów.
Dotyczy to między innymi instancji wykresów oraz rejestratorów zdarzeń.
Niepoprawne usuwanie listenerów lub utrzymywanie niepotrzebnych referencji do obiektów może prowadzić do ich pozostawania w pamięci dłużej, niż jest to wymagane, a w określonych przypadkach do wycieków pamięci.
Wykorzystanie frameworka aplikacyjnego może ułatwić zarządzanie tymi aspektami poprzez komponentyzację, mechanizmy reaktywności oraz zdefiniowane mechanizmy zarządzania cyklem życia.

Z tego względu implementacja z wykorzystaniem Vanilla JS, mimo że jest technicznie możliwa, wiązałaby się w przypadku projektowanego narzędzia z koniecznością samodzielnego rozwiązania problemów związanych z zarządzaniem stanem i zależnościami pomiędzy elementami interfejsu.
Wraz ze wzrostem złożoności aplikacji zwiększałoby to nakład pracy związany z jej rozwojem i utrzymaniem.

\subsection{Technologie i frameworki webowe}

W celu wyboru optymalnego fundamentu dla interfejsu użytkownika, konieczne było przeanalizowanie wiodących rozwiązań na rynku technologii front-endowych.
Współczesny ekosystem aplikacji webowych zdominowany jest przez cztery główne technologie: React, Angular, Vue.js oraz Svelte.
Aby decyzja architektoniczna opierała się na obiektywnych przesłankach, przeanalizowano długoterminowe trendy popularności oraz satysfakcji programistów na podstawie dwóch największych, globalnych badań społeczności IT: \textit{Stack Overflow Developer Survey} z lat 2018--2025 \cite{so_survey_2018, so_survey_2019, so_survey_2020, so_survey_2021, so_survey_2022, so_survey_2023, so_survey_2024, so_survey_2025} oraz \textit{JetBrains The State of Developer Ecosystem} z lat 2017--2023 \cite{jb_survey_2017, jb_survey_2018, jb_survey_2019, jb_survey_2020, jb_survey_2021, jb_survey_2022, jb_survey_2023}.

Analiza historyczna danych ze \textit{Stack Overflow} ukazuje wyraźną ewolucję rynku na przestrzeni lat.
W 2018 roku dominującą technologią był Angular (36,9\%), wyprzedzający bibliotekę React (27,8\%).
W kolejnych latach trend ten uległ silnemu odwróceniu -- React odnotowywał stały wzrost, osiągając w 2025 roku pozycję bezapelacyjnego lidera z wynikiem 44,7\% wskazań w kategorii najpopularniejszych frameworków i technologii webowych.
Angular, po początkowych spadkach, ustabilizował swoją pozycję rynkową na poziomie 17--18\%.
Równolegle, niezwykle silną pozycję ugruntował framework Vue.js, który według badań Stack Overflow wykazuje niemal identyczną adopcję co Angular (17,6\% w 2025 r.)
.
Z kolei raporty \textit{JetBrains}, skupiające się na realnym, codziennym wykorzystywaniu technologii w projektach, pozycjonują Vue.js jeszcze wyżej -- w latach 2021--2023 framework ten był deklarowany przez 32--43\% respondentów, stabilnie zajmując drugie miejsce na rynku, deklasując tym samym Angulara (17--20\%).
Na szczególną uwagę zasługuje również Svelte.
Choć jego rynkowy udział pozostaje relatywnie niewielki (7,2\% według Stack Overflow w 2025 r.)
, od kilku lat niezmiennie przoduje on w rankingach satysfakcji i podziwu (kategorie \textit{Loved/Admired}), osiągając w 2025 roku najwyższy wskaźnik aprobaty na poziomie 62,4\%.

\begin{figure}[ht]
      \centering
      \includegraphics[width=0.80\linewidth]{images/web_frameworks/stackoverflow-dev-survey-2025-technology-admired-and-desired-da-webframe-webframe-desire-admire-social.png}
      \caption{Stack Overflow Developer Survey 2025 -- Web frameworks and technologies - Admired and Desired \cite{so_survey_2025_frameworks} }
      \label{fig:stackoverflow-dev-survey-2025-technology-admired-and-desired-da-webframe-webframe-desire-admire-social}
\end{figure}

Sama popularność rynkowa nie stanowi jednak wystarczającego kryterium doboru w inżynierii oprogramowania.
Porównanie wymienionych technologii wymaga przede wszystkim uwzględnienia różnic w ich modelu programistycznym, koncepcji zarządzania stanem oraz poziomie oferowanej abstrakcji architektonicznej.
Jednym z podstawowych elementów środowiska aplikacji webowych jest Obiektowy Model Dokumentu (\textit{ang. DOM - Document Object Model}), stanowiący programistyczną reprezentację dokumentu HTML w postaci hierarchicznej struktury węzłów.
DOM definiuje sposób reprezentowania oraz programowego odczytywania i modyfikowania elementów dokumentu, umożliwiając między innymi zmianę jego zawartości i struktury za pomocą skryptów \cite{dom_spec}.
Sposób, w jaki poszczególne technologie abstrahują dostęp do DOM oraz zarządzają aktualizacją interfejsu, stanowi jedną z istotnych różnic pomiędzy analizowanymi rozwiązaniami.
W analizie uwzględniono następujące technologie:

\begin{itemize}
      \item \textbf{React} -- w ścisłym ujęciu architektonicznym nie jest frameworkiem, lecz deklaratywną biblioteką służącą do budowy interfejsów użytkownika, rozwijaną pierwotnie przez firmę Meta (dawniej Facebook).
            Wykorzystuje składnię JSX (rozszerzenie języka łączące logikę JavaScript ze strukturą HTML) oraz mechanizm wirtualnego modelu DOM (\textit{ang. Virtual DOM}).
            React skupia się wyłącznie na warstwie widoku, wymagając od programisty samodzielnego doboru zewnętrznych narzędzi do zarządzania trasowaniem (\textit{ang. routing}) czy stanem globalnym, co z jednej strony daje swobodę, a z drugiej utrudnia standaryzację kodu \cite{react_dev_learn}.

      \item \textbf{Angular} -- potężny, monolityczny framework aplikacyjny wspierany przez firmę Google.
            Jest to rozwiązanie typu zawierające baterie w zestawie (\textit{ang. batteries-included}), dostarczające pełen zestaw wbudowanych narzędzi do budowy złożonych systemów.
            Narzuca rygorystyczną architekturę opartą na języku TypeScript, programowaniu zorientowanym obiektowo (klasy, dekoratory) oraz wbudowanym mechanizmie wstrzykiwania zależności (\textit{ang. Dependency Injection}).
            Charakteryzuje się bardzo stromą krzywą uczenia oraz dużym narzutem początkowym \cite{angular_overview}.

      \item \textbf{Vue.js} -- progresywny framework webowy, który wyewoluował jako synteza najlepszych cech Angulara i Reacta.
            Wprowadza elegancką architekturę plików jednokomponentowych (SFC -- \textit{Single-File Components}), w których widok (HTML), style (CSS) i logika (JavaScript/TypeScript) są wyraźnie odseparowane w ramach jednego spójnego pliku.
            Wersja 3 frameworka oferuje zaawansowany i wysoce zoptymalizowany system reaktywności (\textit{Composition API}), pozwalający na automatyczne śledzenie zależności i aktualizowanie interfejsu przy pracy na dużych zbiorach danych, przy jednoczesnym zachowaniu niskiego progu wejścia \cite{vuejs_guide_introduction}.

      \item \textbf{Svelte} -- framework proponujący radykalnie odmienne podejście do budowy interfejsów, całkowicie odrzucając koncepcję wirtualnego modelu DOM.
            Svelte działa jako kompilator, który podczas fazy budowania aplikacji (\textit{ang. build step}) analizuje kod i tłumaczy go na wysoce zoptymalizowane, natywne operacje JavaScript aktualizujące bezpośrednio rzeczywisty model DOM.
            Gwarantuje to ekstremalnie wysoką wydajność, jednak ekosystem dostępnych bibliotek analitycznych i gotowych komponentów pozostaje wyraźnie mniejszy w porównaniu do wyżej wymienionych technologii \cite{svelte_welcome}.
\end{itemize}

\subsection{Meta-frameworki i mechanizmy renderowania}

Przeanalizowane w poprzednim podrozdziale technologie, takie jak React czy Vue.js, dostarczają potężnych narzędzi do budowy komponentów i zarządzania stanem interfejsu.
W praktyce inżynierskiej, stworzenie pełnoprawnej aplikacji webowej wymaga jednak implementacji dodatkowych warstw infrastrukturalnych -- w tym systemu routingu, mechanizmów pobierania danych, czy zaawansowanego procesu budowania i optymalizacji zasobów (z wykorzystaniem narzędzi takich jak Webpack czy Vite).
Konieczność ręcznej integracji i konfiguracji tych elementów nierzadko prowadzi do powstawania niespójnych architektonicznie rozwiązań.

Odpowiedzią na te wyzwania architektoniczne jest zastosowanie meta-frameworków -- wysoce ustrukturyzowanych środowisk zbudowanych na fundamencie podstawowych bibliotek, narzucających ustandaryzowaną architekturę zgodnie z paradygmatem \textit{konwencja ponad konfiguracją} (\textit{ang. Convention over Configuration}).
Do dominujących rozwiązań w tej kategorii należą \textbf{Next.js} (zbudowany wokół biblioteki React) \cite{nextjs_what_is_nextjs} oraz \textbf{Nuxt} (oparty na frameworku Vue.js) \cite{nuxt_introduction}.

Kluczową przewagą zastosowania meta-frameworków, o fundamentalnym znaczeniu dla projektowanej aplikacji analitycznej, jest elastyczność w doborze trybu renderowania aplikacji.
Tradycyjne środowiska implementują zazwyczaj pojedynczy model dystrybucji, co może stanowić istotne ograniczenie.
Meta-frameworki pozwalają natomiast na per-stronnicowe lub globalne definiowanie strategii budowania:

\begin{itemize}
      \item \textbf{CSR (\textit{ang. Client-Side Rendering}):}
            Klasyczne podejście wykorzystywane w aplikacjach typu jednostronicowych (\textit{ang. SPA - Single Page Application}).
            Przeglądarka pobiera pusty dokument HTML oraz skompilowany plik JavaScript.
            Cały proces rysowania interfejsu i pobierania danych odbywa się na urządzeniu końcowym (po stronie klienta).
            Pozwala to na maksymalną interaktywność i płynność działania aplikacji (co jest kluczowe przy obsłudze dynamicznych wykresów analitycznych).

      \item \textbf{SSR (\textit{ang. Server-Side Rendering}):}
            Dokument HTML jest generowany dynamicznie przez serwer w odpowiedzi na każde żądanie użytkownika.
            Choć optymalne z punktu widzenia widoczności w wyszukiwarkach (\textit{ang. SEO - Search Engine Optimization}), podejście to wymaga utrzymywania aktywnej infrastruktury backendowej (Node.js) i nie jest optymalne dla narzędzi zamkniętych (np. w laboratoriach analitycznych).

      \item \textbf{SSG (\textit{ang. Static Site Generation}):}
            Cała aplikacja, wraz z danymi, jest renderowana do statycznych plików HTML, CSS i JS wyłącznie w momencie procesu budowania (\textit{ang. build time}).
            Gotowe zasoby mogą być następnie błyskawicznie i tanio serwowane (hostowane) z wykorzystaniem mechanizmów CDN (\textit{ang. Content Delivery Network}).

      \item \textbf{Renderowanie Uniwersalne (\textit{ang. Universal Rendering}) z procesem uwadniania (\textit{ang. Hydration}):}
            Podejście hybrydowe, natywnie wspierane przez framework Nuxt.
            Pozwala ono na wygenerowanie kompletnego, statycznego kodu HTML (często w trybie SSG) gwarantującego błyskawiczny czas początkowego ładowania.
            Natychmiast po załadowaniu w przeglądarce, skrypty frameworka wykonują proces \textit{uwadniania} (\textit{ang. hydration}), podłączając mechanizmy reaktywności i obsługi zdarzeń.
            Od tego momentu dokument funkcjonuje jako pełnoprawna aplikacja SPA, oferująca maksymalną wydajność bez konieczności odpytywania serwera przy kolejnych interakcjach.
\end{itemize}

Rozbudowane meta-frameworki rozwiązują również problemy zarządzania zasobami i automatycznej strukturyzacji projektu (np. mapowanie struktury folderów bezpośrednio na adresy URL, automatyczny import komponentów), co znacząco obniża dług techniczny i redukuje czas potrzebny na wdrożenie aplikacji.

Zdolność płynnego przejścia ze statycznie wygenerowanej struktury wstępnej (zoptymalizowanej pod szybki start) w wysoce interaktywną aplikację jednostronicową sprawiła, że mechanizmy oferowane przez meta-frameworki stały się pożądanym kierunkiem rozwoju architektury projektowanego narzędzia.

\section{Architektura aplikacji analitycznej w środowisku Nuxt}

Na podstawie przeprowadzonego porównania technologii, jako fundament warstwy aplikacyjnej wybrano meta-framework \textbf{Nuxt.js}.
Rozwiązanie to pozwoliło w pełni wykorzystać potencjał oraz elastyczność ekosystemu Vue.js, eliminując jednocześnie konieczność manualnego konfigurowania środowiska budowania (zintegrowanego z narzędziem \textbf{Vite} \cite{vite_guide}, którego autorem jest \textbf{Evan You} - twórca \textbf{Vue.js}) oraz infrastruktury trasowania.
Kluczowym argumentem przemawiającym za wyborem Nuxta, z punktu widzenia wzorca \textit{konwencja ponad konfiguracją}, była możliwość zautomatyzowania powtarzalnych procesów deweloperskich.
Obejmuje to między innymi mechanizm automatycznego wczytywania modułów i komponentów (\textit{ang. auto-imports}) oraz natywne wsparcie dla silnego typowania w języku TypeScript bez wymogu tworzenia dodatkowych plików konfiguracyjnych.

Jednym z najważniejszych aspektów infrastrukturalnych, który zdeterminował wybór tego frameworka, była jego zdolność do obsługi renderowania hybrydowego (\textit{Hybrid Rendering}) dostarczana przez wbudowany silnik serwerowy \textbf{Nitro}.
W odróżnieniu od klasycznych aplikacji SPA, w których przeglądarka pobiera pusty szkielet i musi samodzielnie zainicjować pobieranie skryptów, aplikacja zrealizowana w środowisku Nuxt generuje kompletny dokument HTML już po stronie serwera docelowego.
Takie podejście znacząco przyspiesza początkowy czas renderowania (\textit{ang. Initial Page Load}) i optymalizuje widoczność narzędzia dla wyszukiwarek (SEO).
Jednak natychmiast po załadowaniu pierwszego widoku w przeglądarce klienta, skrypty frameworka wykonują proces uwadniania (\textit{ang. Hydration}), podłączając mechanizmy reaktywności do statycznego dotąd dokumentu.
Od tego momentu aplikacja płynnie transformuje się w pełnoprawną aplikację jednostronicową (SPA), co gwarantuje natychmiastową reakcję na interakcje użytkownika i elastyczną nawigację bez przeładowywania strony.

Możliwości silnika Nitro pozwoliły na bezproblemowe wdrożenie (\textit{ang. deployment}) środowiska analitycznego w architekturze brzegowej (\textit{ang. Edge Computing}), z wykorzystaniem globalnej infrastruktury \textbf{Cloudflare}.
Dzięki temu pliki konfiguracyjne JSON z wynikami testów oraz sama logika interfejsu mogą być serwowane z fizycznych serwerów zlokalizowanych możliwie najbliżej użytkownika końcowego.
Taka architektura pozwala zachować rygorystyczne oddzielenie ciężkich operacji transformacyjnych i renderowania interaktywnych wykresów Chart.js w pamięci lokalnej klienta od wydajnego dostarczania początkowej struktury widoków, gwarantując maksymalną wydajność całego systemu analitycznego.

\begin{figure}[ht]
      \centering
      \includegraphics[width=0.80\linewidth]{images/nuxt/hero.png}
      \caption{ESPy-Nexus -- Strona główna \cite{espy_nexus_main} }
      \label{fig:espy-nexus-hero}
\end{figure}

\subsection{Zgodność ze wzorcem Separation of Concerns (Struktura projektu)}

Jednym z kluczowych wyzwań przy projektowaniu rozbudowanych paneli analitycznych (tzw.
\textit{Dashboards}) jest narastająca złożoność kodu źródłowego.
Połączenie w jednym pliku procesów manipulacji danymi JSON z deklaracją znaczników wizualnych HTML niemal zawsze prowadzi do degradacji architektury (tzw.
\textit{Spaghetti Code}).
Framework Nuxt narzuca strukturalne rozwiązanie tego problemu, implementując paradygmat podziału odpowiedzialności \textit{ang. Separation of Concerns} poprzez dedykowaną architekturę systemu plików.

W niniejszym projekcie główne procesy aplikacyjne zostały rygorystycznie podzielone na niezależne i odseparowane od siebie katalogi robocze, których poprawne zdefiniowanie automatycznie aktywuje odpowiednie mechanizmy środowiska Nuxt.

Tradycyjne aplikacje SPA (\textit{Single Page Application}) wymagają manualnego konfigurowania modułu trasowania (tzw.
routera) i wiązania ścieżek URL z konkretnymi widokami.
W projektowanej aplikacji analitycznej zrezygnowano z tego obciążenia na rzecz wbudowanego systemu \textit{File-based routing}.
Każdy plik z rozszerzeniem \texttt{.vue} w katalogu \texttt{pages/} automatycznie staje się oddzielną, w pełni nawigowalną trasą aplikacji.
System analityczny podzielono na dwie podstawowe domeny zadaniowe:
\begin{itemize}
      \item \textbf{Panel wizualizacyjny (\texttt{pages/index.vue}):}
            Widok domyślny pod adresem głównym (\texttt{/}), stanowiący centrum dowodzenia eksperymentem (Rys. \ref{fig:espy-nexus-chart}).
            Odpowiada za agregację wszystkich narzędzi filtrujących oraz rysowanie interaktywnych instancji wykresów biblioteki Chart.js.

      \item \textbf{Eksplorator surowych danych (\texttt{pages/data.vue}):}
            Dedykowany widok pod ścieżką \texttt{/data}, zapewniający szybki, tabelaryczny dostęp do kompletnej matrycy wyników analizy (Rys. \ref{fig:espy-nexus-data}).
            Został on zaprojektowany jako element asystujący w celach debugowania warstwy analitycznej.

\end{itemize}

\begin{figure}[H]
      \centering
      \includegraphics[width=0.80\linewidth]{images/nuxt/chart.png}
      \caption{ESPy-Nexus -- Panel wizualizacyjny \cite{espy_nexus_main} }
      \label{fig:espy-nexus-chart}
\end{figure}

\begin{figure}[H]
      \centering
      \includegraphics[width=0.80\linewidth]{images/nuxt/data.png}
      \caption{ESPy-Nexus -- Eksplorator danych HIL \cite{espy_nexus_data} }
      \label{fig:espy-nexus-data}
\end{figure}

Obydwa widoki docelowe są osadzane w głównym szablonie aplikacji zdefiniowanym w pliku \texttt{app.vue}.
Szablon ten (Listing \ref{lst:app_vue}) zapewnia jednolitą strukturę otaczającą obejmującą zunifikowany nagłówek z możliwością wyboru języka interfejsu (moduł \texttt{@nuxtjs/i18n}), nawigację, stopkę oraz ustandaryzowaną konfigurację metatagów optymalizacyjnych dla wyszukiwarek sieciowych (\textit{SEO}).

\begin{listing}[htbp]
      \caption{Szablon główny aplikacji integrujący system trasowania Nuxt (\texttt{app.vue}) \cite{espy_nexus_nuxt_dashboard}.}
      \label{lst:app_vue}
      \begin{minted}[fontsize=\footnotesize, linenos]{html}
<template>
	<UApp :locale="locales[locale]">
		<NuxtRouteAnnouncer />

		<AppHeader />

		<UMain>
			<NuxtLayout>
				<NuxtPage />
			</NuxtLayout>
		</UMain>

		<AppFooter />
	</UApp>
</template>

<script setup lang="ts">
import * as locales from "@nuxt/ui/locale";

const config = useRuntimeConfig();
const { locale, t } = useI18n();

useSeoMeta({
	// basic
	description: () => t("seo.description"),

	// og
	ogTitle: () => t("seo.title"),
	ogDescription: () => t("seo.descriptionLong"),
	ogSiteName: config.public.appName,
	ogType: "website",
	ogImage: "/og-image.png",

	// X
	twitterCard: "summary_large_image",

	// other
	author: "Jakub Niewiarowski",
	themeColor: "#0f172a",
});

const i18nHead = useLocaleHead({ seo: true });

useHead(() => ({
	htmlAttrs: {
		lang: i18nHead.value.htmlAttrs!.lang,
		dir: i18nHead.value.htmlAttrs!.dir,
	},
	link: [...(i18nHead.value.link || [])],
	meta: [...(i18nHead.value.meta || [])],
}));
</script>

\end{minted}
\end{listing}

Warstwa ostatecznej prezentacji oraz mechanika interakcji z użytkownikiem zostały wyizolowane z głównych widoków przy użyciu komponentów oprogramowanych w katalogu \texttt{components/}.
Zgodnie ze standardem biblioteki Vue.js, każdy z komponentów implementuje architekturę plików jednokomponentowych (\textit{ang. SFC -Single-File Components}).
Wzorzec ten hermetycznie grupuje deklarację interfejsu w bloku \texttt{<template>}, logikę sprzężoną z tym widokiem w bloku \texttt{<script setup>} oraz opcjonalne style kaskadowe w wyizolowanym zakresie \texttt{<style scoped>}.

Takie podejście umożliwiło rozbicie potężnego interfejsu panelu analitycznego na serię drobnych, łatwych w testowaniu klocków.
Doskonałym przykładem jest komponent filtrów sterujących osią częstotliwości, który można bez modyfikacji kodu wykorzystywać wielokrotnie w obrębie całego projektu.
Framework Nuxt dokonuje ich asynchronicznego importu.

Wyniki ewaluaanalizy przeprowadzanych przez moduł \texttt{TestEngine} tworzą obszerny zbiór danych w formie bazy danych, ale także pliku \texttt{JSON}.
Wyniki te zostały zmagazynowane jako statyczny zestaw wpisów i osadzone bezpośrednio wewnątrz struktury projektu (w dedykowanym katalogu zasobów statycznych \texttt{assets/data.json}).

Takie rozwiązanie inżynierskie eliminuje obciążenie związane z opóźnieniami sieciowymi przy asynchronicznym żądaniu i pobieraniu danych.
Narzędzie kompilujące Vite natywnie rozwiązuje tę ścieżkę importu już w fazie tworzenia paczki, efektywnie wplatając zbiory surowych wyników bezpośrednio do wykonywalnej warstwy logiki klienckiej.

\subsection{Zarządzanie stanem i reaktywna logika biznesowa}

W architekturze nowoczesnych aplikacji klasy SPA wysoce niewskazane jest osadzanie logiki odpowiedzialnej za przekształcanie, filtrowanie czy agregację danych bezpośrednio w kodzie renderującym interfejs graficzny (tj.
wewnątrz definicji komponentów \texttt{Vue}).
Takie sprzężenie (ang.
\textit{tight coupling}) drastycznie obniża testowalność kodu i prowadzi do wielokrotnego powielania tych samych bloków operacyjnych.

W ramach implementacji panelu analitycznego \textit{ESPy-Nexus}, problem ten rozwiązano poprzez całkowite wyabstrahowanie logiki biznesowej do warstwy tzw.
\textit{Composables}.
Korzystając z mechanizmów \textit{Composition API} dostarczanych przez bibliotekę Vue 3, zaprojektowano wyizolowany, reaktywny stan globalny, hermetycznie zamknięty wewnątrz własnego API: \texttt{useAnalytics()}.

Zdefiniowany w katalogu \texttt{composables/} moduł analityczny przechowuje i udostępnia wszystkim komponentom w systemie kompletny model stanu aplikacji (np. listę wyselekcjonowanych do analizy protokołów, aktualnie wybrany zakres częstotliwości czy rodzaj topologii sieciowej).
Oparcie logiki na funkcyjnym API znacząco podnosi jakość i spójność struktury.

Zamiast implementacji skomplikowanych bibliotek do zarządzania stanem globalnym (takich jak Pinia \cite{pinia_introduction}), framework Nuxt oferuje zoptymalizowane, natywne rozwiązanie -- komendę \texttt{useState()}.
Pozwala ona na zdefiniowanie współdzielonego, reaktywnego obszaru pamięci na bazie unikalnego identyfikatora klucza, gwarantując przy tym ciągłość i stabilność w procesie uwadniania strony (\textit{SSR Hydration}).
Uproszczony wycinek definicji głównego rejestru analitycznego wraz ze wczytaniem danych zaprezentowano na Listingu \ref{lst:use_analytics_state}.

\begin{listing}[htbp]
      \caption{Wyizolowany moduł analityczny definiujący globalny stan z wykorzystaniem funkcji \texttt{useState} (\texttt{useAnalytics.ts}) \cite{espy_nexus_nuxt_dashboard}.}
      \label{lst:use_analytics_state}
      \begin{minted}[fontsize=\footnotesize, linenos]{typescript}
import rawJsonData from "~/assets/data.json";

export interface HilDataRow {
    test_id: number;
    router_topology: string;
    protocol: string;
    freq_hz: number;
    status: string;
    payload_b: number;
    [key: string]: string | number;
}

// Inicjalizacja referencji do wielomegabajtowej tablicy wyników
const rawDataRef = ref<HilDataRow[]>(rawJsonData as HilDataRow[]);

// Współdzielone instancje stanu globalnego (Filtry interfejsu)
export const useAnalytics = () => {
		const selectedTests = useState<string[]>(
		"hil-selected-tests",
		() => [],
	);

	const selectedMetric = useState<string | undefined>(
		"hil-selected-metric",
		() => undefined,
	);

	const xRangeLimit = useState<[number, number]>(
		"hil-x-range-limit",
		() => [0, 1000],
            // Wartości domyślne dla zakresu osi X (częstotliwość w Hz)
            // Zakres ten jest dynamicznie aktualizowany podczas montowania
            // komponentu w oparciu o zbiór danych wejściowych
	);

	const selectedXRange = useState<[number, number]>(
		"hil-selected-x-range",
		() => [0, 1000],
	);
      // [...]
}
    
\end{minted}
\end{listing}

Drugim z kluczowych mechanizmów architektonicznych wprowadzonych na poziomie kontrolera \texttt{useAnalytics} były \textit{Własności Obliczane} (\textit{ang. Computed Properties}).
Ze względu na fakt, że plik wejściowy JSON może zawierać dziesiątki tysięcy rekordów o wielu stopniach parametryzacji, klasyczne i powtarzalne iterowanie przez całą tablicę podczas zmiany wybranego filtra stanowiłoby ogromne obciążenie dla procesora maszyny klienckiej.

Reaktywny mechanizm \texttt{computed()} wykorzystany w architekturze Vue buduje wewnętrzny układ zależności (tzw.
graf drzewa stanów).
Pule z dostępnymi dla inżyniera opcjami wyboru testów (np. \texttt{availableTopologies} czy \texttt{availableProtocols}) są obliczane wyłącznie jeden raz w momencie wczytania struktury JSON, a wyniki te zostają zbuforowane ( \textit{ang. cached}) w wewnętrznej pamięci podręcznej frameworka.
Dopóki macierz danych podstawowych (\texttt{rawDataRef}) nie ulegnie modyfikacji, co w środowisku statycznym nie następuje, silnik omija bezcelowe przeliczanie list filtrów (Listing \ref{lst:use_analytics_computed}).
Oszczędność cykli obliczeniowych w środowiskach wykonawczych JavaScript jest warunkiem koniecznym do poprawnego wyrenderowania wykresów HIL w częstotliwości 60 klatek na sekundę.

\begin{listing}[htbp]
      \caption{Mechanika agregacji i buforowania danych słownikowych (\texttt{useAnalytics.ts}) \cite{espy_nexus_nuxt_dashboard}.}
      \label{lst:use_analytics_computed}
      \begin{minted}[fontsize=\footnotesize, linenos]{typescript}
	const availableTests = computed(() => {
		const names = rawDataRef.value.map(
			row => `${row.protocol}_${row.router_topology}_${row.payload_b}b`
		);
		return [...new Set(names)];
	});

    	const availableTopologies = computed(() => {
		const topologies = rawDataRef.value.map(
			row => row.router_topology,
		);
		return [...new Set(topologies)];
	});
\end{minted}
\end{listing}

Przeniesienie całkowitej odpowiedzialności za selekcję rekordów, przeliczanie osi dla wykresów oraz definiowanie domyślnych opcji (\textit{Presets}) do scentralizowanego pliku logiki biznesowej pozwala zachować spójność strukturalną interfejsu.
Komponenty wizualne zajmują się w efekcie wyłącznie rysowaniem zadeklarowanych stanów.

\subsection{Projektowanie warstwy prezentacyjnej (Interfejs Użytkownika)}

W tradycyjnym procesie budowania interfejsów webowych, deweloperzy latami polegali na kaskadowych arkuszach stylów (CSS) organizowanych w zewnętrznych plikach.
Z biegiem czasu, w celu przyspieszenia prac nad powtarzalnymi elementami aplikacji, standardem branżowym stało się wykorzystywanie potężnych bibliotek komponentów, z których najpopularniejszą jest \textbf{Bootstrap} \cite{bootstrap_getting_started}.
Zrewolucjonizował on podejście do projektowania oferując gotowe zestawy klas (np. \texttt{btn btn-primary}) oraz zintegrowane skrypty JavaScript obsługujące logikę komponentów (np. modali czy siatek responsywnych), które można wdrożyć błyskawicznie z wykorzystaniem sieci CDN.
Jednakże, monolityczny charakter takich bibliotek często prowadzi do problemów związanych z generycznością komponentów, nadpisywaniem stylów w obrębie projektu oraz trudnościami w dostosowywaniu gotowych elementów do indywidualnych potrzeb projektowych.

Odpowiedzią na te problemy stało się wprowadzenie metodologii \textit{Utility-First}, spopularyzowanej przez kompilator \textbf{Tailwind CSS} \cite{tailwindcss_installation_vite}.
Dostarcza on potężny zestaw niskopoziomowych klas narzędziowych (np. \texttt{flex, text-center, bg-red-500}), które aplikuje się bezpośrednio w strukturze znaczników HTML.
podobnie jak w przypadku styli Bootstrap.
Takie podejście drastycznie zwiększa elastyczność i eliminuje problem globalnego nakładania się stylów, ponieważ style komponentu są zdefiniowane i zamknięte w miejscu jego występowania.
Co niezwykle istotne, Tailwind działa w oparciu o mechanizm JIT (\textit{ang. Just-In-Time compilation}) -- silnik skanuje pliki źródłowe, wykrywając użyte klasy, i generuje plik CSS zawierający wyłącznie ten kod, który faktycznie został użyty.
Gwarantuje to niezwykle niską wagę paczki wynikowej aplikacji produkcyjnej (tzw.
zero-runtime).

O ile Tailwind rozwiązuje problem stylizacji graficznej, o tyle nie dostarcza on biblioteki komponentów, mechanizmów dostępności cyfrowej (\textit{Accessibility}) ani obsługi zdarzeń dla złożonych elementów nawigacyjnych.
Z tego względu jako docelowy fundament warstwy wizualnej środowiska \textit{ESPy-Nexus} wybrano \textbf{Nuxt UI}.
Jest to natywna, wszechstronna biblioteka interfejsu (współpracująca również z czystym środowiskiem Vue), dostarczająca ponad 125 gotowych do użycia, wysoce responsywnych komponentów.
Łączy ona w sobie swobodę projektowania graficznego z kompilatora Tailwind CSS z rygorystycznymi standardami inżynierii oprogramowania \cite{nuxt_ui_getting_started}.

Kluczowym aspektem architektury Nuxt UI jest zastosowanie silnika \textbf{Reka UI} jako warstwy fundamentalnej (\textit{ang. primitive layer}).
Warto nadmienić, że mechanika ta wywodzi się historycznie z popularnego w ekosystemie React projektu \textit{Radix UI}, z którego następnie wydzielono dedykowaną implementację zoptymalizowaną specjalnie pod system reaktywności frameworka Vue (Reka UI).
Integracja ta gwarantuje natywną dostępność projektowanego panelu:
\begin{itemize}
      \item \textbf{Zgodność ze standardem WAI-ARIA:}
            Komponenty Nuxt UI posiadają wbudowaną semantykę ról, co pozwala na bezproblemową nawigację czytnikom ekranowym (\textit{ang. Screen Readers}).
      \item \textbf{Inteligentne zarządzanie fokusem:}
            Komponenty złożone, takie jak okna dialogowe (\texttt{UModal}) czy akordeony (\texttt{UAccordion}), automatycznie zarządzają przenoszeniem uwagi (fokusu) po ich aktywacji, co znacząco poprawia komfort nawigacji z poziomu klawiatury.
      \item \textbf{Keyboard Navigation:}
            Wbudowane wsparcie klawiatury dla złożonych komponentów, takich jak zakładki i okna dialogowe.
\end{itemize}

Wykorzystanie pakietu Nuxt UI pozwoliło wdrożyć spójny system projektowy oparty na palecie siedmiu kolorów semantycznych (\textit{primary, secondary, success, info, warning, error, neutral}).
Eliminuje to konieczność używania bezpośrednich wartości kolorystycznych (tzw.
\textit{Hardcoding}) i gwarantuje płynną obsługę wbudowanego trybu ciemnego i jasnego (\textit{Color Mode}).
Przykład wykorzystania gotowych kontrolerów do budowy zaawansowanego panelu filtrów, prezentujący deklaratywną integrację przycisków (\texttt{UButton}) oraz grupy checkboxów (\texttt{UCheckboxGroup}), wplecione w menu akordeonowege (\texttt{UAccordion}), został zaprezentowany na Listingu \ref{lst:data_explorer_filters}.

\begin{listing}[htbp]
      \caption{Deklaratywna budowa interfejsu sterującego z użyciem zoptymalizowanych komponentów Nuxt UI (\texttt{DataExplorerFilters.vue}) \cite{espy_nexus_nuxt_dashboard}.}
      \label{lst:data_explorer_filters}
      \begin{minted}[fontsize=\footnotesize, linenos]{html}
<template>
	<UAccordion
		:items="accordionItems"
		type="multiple"
		:default-value="['tests', 'presets', 'metric']"
		class="w-full"
	>
		<template #tests>
			<div class="max-h-72 overflow-y-auto p-1 mt-1 mb-2
                  scrollbar-thin">
				<UCheckboxGroup
					v-model="selectedTests"
					:items="availableTests"
					variant="table"
					size="sm"
					color="primary"
				/>
			</div>
		</template>

		<template #presets>
			<div class="flex flex-col gap-4 my-3 px-1 max-h-[60vh]
                  overflow-y-auto scrollbar-thin">
				<div class="grid grid-cols-2 gap-2">
					<UButton
						:label="$t('dataExplorer.filters.all')"
						icon="heroicons:check-circle"
						color="primary"
						variant="soft"
						class="justify-center"
						@click="selectAllTests"
					/>
                        <!-- [...] -->
\end{minted}
\end{listing}

Dzięki natywnemu wsparciu dla języka TypeScript, aplikacja korzysta z pełnej kontroli typów (w tym bezpieczeństwa przy podawaniu własności komponentów w szablonach), a programista jest wspierany przez wbudowane mechanizmy autouzupełniania (\textit{IntelliSense}) we wszystkich elementach struktury GUI.

Integralną częścią aplikacji jest podstrona \texttt{/data}, pełniąca rolę deweloperskiej przestrzeni weryfikacyjnej.
Wymagała ona zaimplementowania mechanizmu prezentacji w postaci tabelarycznej tysięcy połączonych rekordów wyników uzyskanych w toku analizy, obejmujących między innymi parametry związane ze zjawiskiem jitteru oraz pomiarem przepustowości PDR.
Wykorzystanie standardowego elementu HTML \texttt{<table>} nie zapewnia mechanizmów takich jak filtrowanie, sortowanie czy paginacja, dlatego ich implementacja wymagałaby stworzenia dodatkowej logiki aplikacyjnej.

Problem rozwiązano z wykorzystaniem biblioteki TanStack Table (\texttt{@tanstack/vue-table}) - bezgłowego (\textit{ang. headless}) silnika do obsługi tabel.
Podejście headless oznacza, że biblioteka dostarcza logikę, stan oraz interfejs programistyczny odpowiedzialny za operacje na danych, takie jak filtrowanie, sortowanie i paginacja, nie narzucając jednocześnie struktury HTML ani warstwy wizualnej komponentu.
Dzięki temu sposób prezentacji danych pozostaje w pełni kontrolowany przez aplikację.
Wykorzystane w implementacji modele wierszy umożliwiają kolejne przekształcanie zbioru danych, między innymi poprzez filtrowanie, sortowanie oraz paginację, a następnie przekazanie wynikowego zbioru wierszy do warstwy prezentacji (Rys. \ref{fig:espy-nexus-data-table}).
Rozwiązanie to jest w pełni zintegrowane z komponentem Nuxt UI \texttt{<UTable>} (Listening \ref{lst:data_vue_table}).

\begin{figure}[htbp]
      \centering
      \includegraphics[width=0.80\linewidth]{images/nuxt/data_table.png}
      \caption{ESPy-Nexus -- Tabela danych \cite{espy_nexus_data} }
      \label{fig:espy-nexus-data-table}
\end{figure}

\begin{listing}[htbp]
      \caption{Deklaratywne podłączanie silnika TanStack Table do interfejsu wizualnego Nuxt UI (\texttt{data.vue}) \cite{espy_nexus_nuxt_dashboard}.}
      \label{lst:data_vue_table}
      \begin{minted}[fontsize=\footnotesize, linenos]{html}
<UTable
	ref="table"
	v-model:pagination="pagination"
	v-model:global-filter="globalFilter"
	v-model:column-visibility="columnVisibility"
	:data="data"
	:columns="columns"
	:pagination-options="{
		getPaginationRowModel: getPaginationRowModel(),
	}"
	class="flex-1"
/>
\end{minted}
\end{listing}

\subsection{Wizualizacja wyników i rozszerzanie natywnych bibliotek (Chart.js)}

Zasadniczym celem stworzonego środowiska webowego jest sprawna i interaktywna wizualizacja parametrów pracy sieci bezprzewodowych.
Wymagało to zastosowania biblioteki graficznej zdolnej do natychmiastowego renderowania tysięcy punktów czasowych w przeglądarce, bez znaczącego spadku wydajności interfejsu (utrzymanie \textit{framerate'u} na poziomie 60 klatek na sekundę).
W tym celu zaimplementowano bibliotekę \textbf{Chart.js} \cite{chartjs_documentation} -- wiodące rozwiązanie wykorzystujące element HTML5 \texttt{<canvas>}.

Integracja zewnętrznej, natywnej biblioteki JavaScript z nowoczesnym cyklem życia frameworka Vue (\textit{Component Lifecycle}) została zrealizowana przy pomocy lekkiego mechanizmu pośredniczącego (\textit{ang. wrapper}) dostarczonego przez pakiet \texttt{vue-chartjs}.
Pozwala to na automatyczne nasłuchiwanie zmian w danych wejściowych i płynną re-alokację pamięci na obiekcie \texttt{<canvas>}, co całkowicie eliminuje niebezpieczeństwo wycieków pamięci podczas częstego przerysowywania widoków.

Wizualizacja wyników z pętli testów typu Hardware-in-the-Loop obnażyła istotny problem ergonomiczny.
Przy włączeniu podglądu dla wielu topologii sieciowych i jednoczesnym przedstawieniu tysięcy wyników analizy na jednym wykresie, punkty danych znajdują się blisko siebie na osi X (częstotliwości) w domyślnym trybie interakcji biblioteki Chart.js \texttt{nearest} ukaże wartość dla punktu jedynie podczas fizycznego najechania kursorem na jego obszar.
Alternatywnie, tryb \texttt{index} umożliwia wyświetlenie wszystkich punktów w danej kolumnie.
Żaden z tych trybów nie jest jednak w pełni ergonomiczny, ponieważ w przypadku gęsto upakowanych danych, szczególnie gdy wartości przebiegów nakładają się, najechanie kursorem na obszar wykresu albo ukaże jedynie pojedynczy punkt, albo \texttt{tooltip} zostanie wyświetlony dla wszystkich punktów w kolumnie, co całkowicie zaciemnia widok.
W celu poprawy komfortu analizy wizualnej, wprowadzono niestandardowy tryb interakcji o nazwie \texttt{hilMode}, który łączy zalety obu natywnych trybów.
W tym trybie, po najechaniu kursorem na obszar wykresu, algorytm wyszukuje punkt danych leżący najbliżej kursora w osi Y (opóźnienie) i wyświetla jego etykietę informacyjną.
Jak zaprezentowano na Listingu \ref{lst:chartjs_hilmode}.
mechanizm ten modyfikuje natywną funkcję \texttt{Interaction.modes.index}, wprowadzając matematyczną tolerancję i progi zderzeń (stałe \texttt{MAX\_TOOLTIP\_DISTANCE\_Y} oraz \texttt{TOOLTIP\_OVERLAP\_TOLERANCE\_Y}).
Dzięki tej ingerencji, po najechaniu kursorem na gęsto upakowany obszar, etykiety informacyjne aktywowane są wyłącznie dla tych przebiegów sygnału, których fizyczna wartość osi Y znajduje się w akceptowalnej bliskości położenia wskaźnika myszy (wartość \texttt{eventY}).
Pozostałe punkty leżące w tej samej kolumnie (na osi X), lecz oddalone na osi Y, są odrzucane z tablicy aktywnych elementów.

\begin{listing}[htbp]
      \caption{Przeciążenie przestrzeni nazw Chart.js i implementacja zaawansowanego algorytmu kolizyjnego dla Tooltipów (\texttt{DynamicLineChart.vue})}
      \label{lst:chartjs_hilmode}
      \begin{minted}[fontsize=\footnotesize, linenos]{typescript}
// Rozszerzenie deklaracji typów biblioteki zewnętrznej
declare module "chart.js" {
	interface InteractionModeMap {
		hilMode: (
                  chart: ChartJS, 
                  e: ChartEvent, 
                  options: InteractionOptions, 
                  useFinalPosition?: boolean) => ActiveElement[];
	}
}

const MAX_TOOLTIP_DISTANCE_Y = 20;
const TOOLTIP_OVERLAP_TOLERANCE_Y = 5;

// Nadpisanie zachowania natywnego w silniku Chart.js
Interaction.modes.hilMode = function (
	chart: ChartJS,
	e: ChartEvent,
	options: InteractionOptions,
	useFinalPosition?: boolean,
): ActiveElement[] {
	const indexItems = Interaction.modes.index(chart, e, options, useFinalPosition);
	if (!indexItems || indexItems.length === 0) return [];

	let minDistanceY = Number.POSITIVE_INFINITY;

	const eventY = e.y ?? 0;

      // Krok 1: Wyszukiwanie punktu danych położonego najbliżej kursora w osi Y
	indexItems.forEach((item: ActiveElement) => {
		const distanceY = Math.abs(item.element.y - eventY);
		if (distanceY < minDistanceY) {
			minDistanceY = distanceY;
		}
	});

      // Jeżeli najbliższy punkt leży poza strefą akceptacji, nie wyświetlaj podglądu
	if (minDistanceY > MAX_TOOLTIP_DISTANCE_Y) return [];

      // Krok 2: Filtracja odrzucająca punkty, które nakładałyby się na ekranie
	return indexItems.filter((item: ActiveElement) => {
		const distanceY = Math.abs(item.element.y - eventY);
		return Math.abs(distanceY - minDistanceY) <= TOOLTIP_OVERLAP_TOLERANCE_Y;
	});
};
\end{minted}
\end{listing}
